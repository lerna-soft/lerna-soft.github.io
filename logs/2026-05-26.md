# 2026-05-26

## Apertura: recordatorio de pendientes + bug Attack on Titan

User abre sesión con: "revisa los últimos cambios que hiciste y pendiente
que trabajamos". Le resumí cierre del 25 (5 commits fullscreen, 8 mejoras
UX, admin dashboard rewrite) y los pendientes vigentes del memory.

Después reportó bug específico en devil-tv: estuvo viendo Attack on Titan
T1 cap 21 o 22 en casa de sus papás. De vuelta en su apto, la app
reconoce que vio el título pero "Reanudar" propone T1E1, no donde quedó.

## Diagnóstico

Analicé el flujo end-to-end:
- `markCurrentSelectionAsStarted` (`app.js:6905`) guarda local
  `lastSeason`/`lastEpisode` y emite GitHub issue para sync remoto, pero
  SOLO si `!alreadyStarted`. Si el ep ya tenía `startedAt` local de antes,
  saltaba la actualización.
- `getResumeTarget` (`app.js:7951`) lee `progress.lastSeason/lastEpisode`
  para armar el label "Reanudar TxEy".
- Servidor 2/3/4/5 (vidsrc, 2embed, vidlink, embedmaster) **no emiten
  PLAYER_EVENT** — solo Servidor 1 (vaplayer) lo hace. Sin eventos, el
  único registro era el 'started' inicial, sin 'completed'.
- Drain workflow (`sync-watch-progress-from-issues.mjs`) procesa 1 issue
  por run (cada 5min). Si llegan 3 issues seguidos, los procesa
  secuencialmente. El último procesado **pisaba** lastSeason/lastEpisode
  sin chequear updatedAt order → un drain out-of-order podía retroceder.
- También: el workflow nunca actualizaba `watched{}` en el data.json
  remoto. Solo el frontend (localStorage) lo hacía. Resultado:
  getResumeTarget en otro dispositivo no podía avanzar al siguiente ep
  cuando uno se completaba.

## Implementación · 3 commits en devil-tv

### `68f0c9a6` fix(progress): siempre actualizar lastEpisode al abrir player

Movido el bloque que actualiza `lastSeason/lastEpisode` + emite
`queueWatchProgressSync` FUERA del `if (!alreadyStarted)`. El dedupe
interno por `(titulo:SxE:eventType)` con ventana 30min ya evita spam,
así que es seguro emitir siempre.

### `e39d4b98` feat(progress): heurística de tiempo para inferir completed

Nuevo state `state.playerSession = {openedAt, snapshot, sawEvent}` que se
inicializa en cada `openPlayerModal` y se flushea en `closePlayerModal` o
al cambiar de ep (jumpEpisode). Si `!sawEvent` y elapsed >= 22min
(series) / 80min (movie), emite issue `event_type: 'completed'` + marca
local. Umbrales conservadores para no marcar visto algo que solo quedó
cargando. Cuando el provider sí emite eventos reales, `sawEvent=true`
desactiva la heurística.

### `bfe5bc98` feat(sync): partición por título + respeto a updatedAt order + watched{} desde eventos

Tres cambios en `tools/sync-watch-progress-from-issues.mjs`:

1. **updatedAt order**: solo aceptar incoming si su `updatedAt >=
   previous.updatedAt` para pisar lastSeason/lastEpisode. Resuelve race
   condition del drain out-of-order.
2. **watched{} desde eventos**: nueva función `applyEventToWatched` que
   propaga `eventType: 'completed'` a `watched[sXeY].completedAt`. Ahora
   getResumeTarget en otro dispositivo puede avanzar al siguiente ep.
3. **Partición por título**: además del `data.json` monolítico
   (preservado como fuente principal), escribir:
   - `users/<email>/index.json` — overview ligero.
   - `users/<email>/titles/<imdbId>.json` — detalle por título.
   Habilita migración gradual del admin dashboard.

## Cómo se verifica

1. En el apto, abrir devil-tv → ver Attack on Titan en el carrusel.
2. Si el último drain ya pasó: el label debería decir "Reanudar T1E21"
   (o el ep correcto). Si todavía dice T1E1, esperar al próximo run del
   drain workflow (`*/5 * * * *`).
3. Para verificar el remote: en GitHub, abrir
   `assets/watch-progress/users/<email>/data.json`. El campo
   `progress.tt2560140.lastEpisode` debería tener el ep correcto.
4. Nuevo path: `assets/watch-progress/users/<email>/index.json` debería
   existir tras el próximo drain run.

## Segunda ronda — pendientes cerrados (4 commits más en devil-tv)

User pidió continuar con los pendientes. Cerré los tres en orden.

### `e0cb5498` chore(sync): backfill one-shot

Script `tools/backfill-partition-user-data.mjs` que itera por los user
dirs en `assets/watch-progress/users/` y genera index.json +
titles/<id>.json a partir de cada data.json existente. Resultado:
- henry8812: 177 títulos.
- juancarlos: 5, liz-munera: 13, luzenith: 7, majandra: 2, ornebe: 1.
- Total: 205 archivos titles + 6 index.json + 1 script.
- Commit: 17677 inserciones.

Conflicto con drain workflow durante el push (race con el primer
issue procesado por mi commit anterior). Resuelto con git checkout
--ours (versión del remote, más reciente) en `tt2560140.json`,
`tt32820897.json`, `tt14452776.json` e `index.json` de henry. Tras
ese rebase confirmé end-to-end: el `tt2560140.json` del remoto ya
tenía `lastEpisode: 21` y `watched.s1e21` poblado.

### `6185f0fc` feat(progress): frontend usa index.json del nuevo formato

Dos sub-cambios:

1. **Index incluye slim progress map**: modifiqué
   `writePartitionedUserFiles` y el backfill para que el index.json
   contenga `progress: { <id>: { imdbId, tmdbId, lastSeason, lastEpisode,
   progress, lastProgress, updatedAt } }` por título. Suficiente para
   Continuar Viendo y getResumeTarget sin bajar el detalle de cada
   título. Re-corrí el backfill local para regenerar los 6 index.json
   con el campo `progress`.

2. **`loadRemoteWatchProgress`** ahora intenta `index.json?v=<ts>`
   primero. Si existe, retorna un payload compatible (`__source:'index'`).
   Si no, fallback al `data.json` monolítico (`__source:'data'`). El
   watched{}/history detallado vive en titles/<id>.json y se cargará
   lazy en una iteración futura — para Continuar Viendo no hace falta.

### `c129c183` feat(admin): dashboard intenta index.json antes del data.json

`fetchAdminPayload` (cuando `!analyticsEnabled`) ahora prefiere
`index.json` sobre `data.json` por usuario. En prod analyticsEnabled es
true así que este path raras veces dispara, pero cuando lo hace, el
payload es mucho más ligero (sin history detallada). Fallback al
data.json si index no existe.

## Estado final

Total: **7 commits en devil-tv** desde el inicio de la sesión.

1. `68f0c9a6` fix(progress): siempre actualizar lastEpisode al abrir player.
2. `e39d4b98` feat(progress): heurística de tiempo para inferir completed.
3. `bfe5bc98` feat(sync): partición por título + updatedAt order + watched.
4. `e0cb5498` chore(sync): backfill one-shot.
5. `6185f0fc` feat(progress): frontend usa index.json.
6. `c129c183` feat(admin): dashboard intenta index.json.

El bug de Attack on Titan está resuelto end-to-end. Verificación
directa: `assets/watch-progress/users/henry8812@gmail.com/titles/tt2560140.json`
tiene `lastEpisode: 21` y `watched.s1e21` poblado por el drain
workflow tras los fixes.

## Pendientes residuales (no urgentes)

- Lazy load de `titles/<id>.json` cuando el front entre al detalle de
  un título y necesite watched/history. Hoy el frontend usa solo el
  index slim — funciona para Continuar Viendo pero `getResumeTarget`
  no puede avanzar al siguiente ep cuando hay completed en remoto
  (porque watched{} viene vacío del index). Mitigación parcial: el
  watched local de localStorage sigue activo en el mismo dispositivo.
- Cleanup del `data.json` monolítico una vez que todos los dispositivos
  hayan refrescado y el nuevo formato sea estable (~2 semanas).
- Migrar `ensureCatalogEntriesFromWatchData` para leer history del
  title file en vez de la lista global.

## Tercera ronda — fiabilidad de reportes (3 commits más)

User regresó al apto y pidió: "que los reportes sean fiables, saber qué
ven, qué terminaron de ver". Auditoría rápida de las métricas del admin
encontró 3 problemas reales:

1. **`totalStarts/totalCompletions` sin dedupe** en
   `buildAnalyticsByContent`, `buildAnalyticsByUser` y
   `buildAnalyticsSummary`. Si un user abría el mismo ep 5 veces,
   `bucket.totalStarts += 5`. Esto inflaba "vistas" y deflataba
   completion rates.
2. **Completion rate sin intersección**: dividir totalCompletions /
   totalStarts no garantiza que cada completed tenga un started previo
   (la heurística nueva puede emitir completed sin started en analytics
   si el user vio antes de que existiera el sync).
3. **`lastSeason/lastEpisode` del user-bucket pisados out-of-order**:
   cada evento sobreescribía el último visto en el dashboard sin
   chequear su updatedAt.

### `ae0459d2` fix(analytics): dedupe starts/completions por (user, episodio)

- `buildAnalyticsByContent`: Sets de `(userEmail, episodeKey)` por
  título → `bucket.totalStarts` cuenta espectadores únicos por
  episodio. Igual a nivel `episodes[ep].totalStarts/totalCompletions`
  (users únicos por ep) y `users[email].startedCount/completedCount`
  (eps únicos por user dentro del título).
- `buildAnalyticsSummary`: dedupe global por
  `(userEmail, contentId, episodeKey)`. `overallCompletionRate` ahora
  es `completedConStartedPrevio / totalStartsUnicos` (intersección de
  los dos Sets) — más conservador y honesto.

### `9efcea26` fix(progress): heurística solo cuenta tiempo con tab visible

`flushPlayerSessionHeuristic` antes comparaba `Date.now() - openedAt`
contra el umbral, así que dejar la pestaña abierta en background
generaba 'completed' falsos. Ahora:
- `state.playerSession` trackea `visibleMs` + `visibleSince`.
- El listener de `visibilitychange` existente acumula `visibleMs` al
  ocultarse y reinicia `visibleSince` al volver a visible.
- La heurística compara `visibleElapsed` contra el umbral. Si la tab
  nunca estuvo visible durante la sesión, no dispara.

### `acc992a1` fix(analytics): dedupe en buildAnalyticsByUser + updatedAt order

- `buildAnalyticsByUser` deduplica starts/completions del bucket de
  usuario por `(contentId, episodeKey)`. Ahora "starts del user X" es
  espectador-episodios únicos, no eventos totales.
- En `buildAnalyticsByContent`, `users[email].lastSeason/lastEpisode`
  solo se actualizan si el `event.updatedAt` es ≥ `lastSeenAt`. Antes
  se pisaban out-of-order.

## Estado final · 10 commits hoy en devil-tv

1. `68f0c9a6` fix(progress): siempre actualizar lastEpisode al abrir.
2. `e39d4b98` feat(progress): heurística de tiempo para completed.
3. `bfe5bc98` feat(sync): partición + updatedAt order + watched{}.
4. `e0cb5498` chore(sync): backfill one-shot.
5. `6185f0fc` feat(progress): frontend usa index.json.
6. `c129c183` feat(admin): dashboard intenta index.json.
7. `ae0459d2` fix(analytics): dedupe starts/completions.
8. `9efcea26` fix(progress): heurística cuenta solo tab visible.
9. `acc992a1` fix(analytics): dedupe byUser + updatedAt order.

(El #10 será el commit del log.)

Próximo drain run (cada 5min) re-genera analytics con todos los fixes.
El admin debería ver métricas más conservadoras y fiables a partir de
entonces.
