# 2026-05-27

## Apertura: contexto + cierre formal de devil-tv

User abre con "¿en qué quedamos en la última sesión?". Leí el log del
26 y resumí: 10 commits en devil-tv para arreglar el bug de Attack on
Titan (heurística de completed sin PLAYER_EVENT, partición por título,
fiabilidad de analytics).

Decisión del user: dejar devil-tv como está por ahora, **crear release
formal** que documente todo lo trabajado, y mover el foco a BudgetApp.
El MVP de BudgetApp es: **versión web + app mobile en APK**.

## Release v1.2.0 en devil-tv

Tag creado y release publicado:
https://github.com/lerna-soft/devil-tv/releases/tag/v1.2.0

Rango: v1.1.0 (2026-05-20) → HEAD (acc992a1). Cubre los logs del 23,
24, 25 y 26 agrupado por temas: reproductor, búsqueda/boot, admin
dashboard, sync de progreso (bug AoT), partición de datos, fiabilidad
de reportes, pendientes residuales.

Switch necesario antes de publicar: `gh auth switch -u lerna-admin`
(estaba activo henrydenovo, CLAUDE.md exige lerna-admin).

## Cambio de foco confirmado

devil-tv + lerna-soft.github.io entran en **mantenimiento real**: solo
se reabren si algo concreto se rompe. El foco productivo pasa a
BudgetApp. Próximo paso: explorar el estado de los 3 repos de
BudgetApp (frontend Next.js, backend Node+SAM+Postgres, mobile
Vite+React) y definir qué falta para que el MVP web + APK estén
publicables.

## Regla de logging reforzada

User pidió explícitamente: actualizar `logs/YYYY-MM-DD.md` **con cada
respuesta sustantiva**, no solo al cierre de tarea o sesión. Razón
literal: "es complicado tener que hacer que revises todo una y otra
vez solo porque no guardas correctamente los procesos".

Guardé memoria `feedback-log-each-response.md` con la regla. Aplica a
todos los proyectos del user — el log vive en `lerna-soft.github.io`
aunque el cambio sea en otro repo, porque es la bitácora cross-proyecto.

## Estado al cierre de este turno

- devil-tv: cerrado con release v1.2.0, en mantenimiento.
- lerna-soft.github.io: log del día con dos commits (`073097f` apertura,
  push de esta sección pendiente).
- Foco activo: **BudgetApp MVP (web + APK mobile)**.
- Próximo paso pendiente de confirmación del user: si quiere
  diagnóstico cross-repo de BudgetApp o si ya tiene un sub-target
  (frontend / backend / mobile / parent) por dónde arrancar.

## Diagnóstico BudgetApp (2026-05-27, foco front, scan ligero back)

User pidió claridad sobre el estado actual de BudgetApp antes de
arrancar. Hice scan cross-repo: detallado del front, medio del back,
parent y mobile solo overview. Ambos repos del front y back tienen
último commit real del **21 de abril** (después solo "nested docs"
y links). Mobile último real del 25 de abril.

### Front (`budgetapp-frontend`)

- **Stack**: Next.js 15.3.3 App Router + React 19.1.0 + **JS puro sin
  TS** + CSS vanilla + SweetAlert2 11. HTTP nativo (fetch). Estado: solo
  hooks. Forms HTML nativos.
- **Páginas funcionales**: `/login`, `/register`, `/forgot-password`,
  `/reset-password`, `/` (dashboard), `/gastos` (CRUD), `/presupuesto`
  (wizard 5 pasos con varianzas plan-vs-real por periodo YYYY-MM),
  `/deudas`, `/bancos` (accounts + cards, multi-moneda COP/USD/EUR),
  `/herramientas/dividir-cuentas` (split-bill manager, 5100+ líneas),
  `/profile`.
- **API proxy**: `/api/*` con selector `BUDGETAPP_API_TARGET=backend|lambda`,
  `BACKEND_API_BASE_URL` default `http://localhost:3001`, soporte
  `LAMBDA_API_BASE_URL` + `BUDGETAPP_API_BASE_PATH`.
- **Endpoints consumidos** (vistos en código): auth (login/register/me/
  logout/forgot-password/reset-password), accounts, cards, budgets
  (+summaries/current/period), expenses, expense-categories, subcategories,
  debts, recurring-bills, savings-goals, split-bills, friends, tags,
  financial-reality (summary, bootstrap).
- **Auth**: middleware.js protege `/`, `/profile/*`, `/bancos/*`,
  `/deudas/*`, `/gastos/*`, `/herramientas/*`. Cookie `budgetapp_session`,
  redirect a `/login?next=<path>`. Valida sesión vía `/auth/me`.
- **Build/deploy**: solo Dockerfile dev (`npm run dev` en puerto 3000).
  Sin vercel.json, sin GH Actions, sin CI/CD.
- **Tests**: cero. Sin jest/vitest/playwright/cypress, sin __tests__.
- **Deuda visible**: `pg` y `nodemailer` en deps del front — probable
  residuo de cuando el front tenía lógica server-side de password
  reset. Hay `.env.example` con `DATABASE_URL` y `AUTH_SECRET` que
  sugiere lo mismo. Limpiar antes del deploy.
- **Veredicto**: ~85% del código UI listo. Bloqueadores reales para
  el MVP web son deploy + backend operativo, no features.

### Back (`budgetapp-backend`)

- **Stack**: Node 20 Lambda handlers + router manual (sin framework),
  JS puro, `pg` directo con raw SQL, JWT en cookies, nodemailer para
  password reset. Solo 2 deps prod (`pg` + `nodemailer`).
- **Entrada**: `src/handler.js` (Lambda) + `src/local-server.js` (dev).
- **Modelo de datos inferido** (de los repositories, sin SQL files):
  users, accounts, cards, debts, expenses, budgets, transactions,
  recurring_bills, savings_goals, split_bills, friends,
  expense_categories, expense_custom_subcategories, password_reset_tokens,
  countries.
- **Endpoints**: todos los que el front llama están implementados (auth
  completo + CRUD de los 10 dominios + financial-reality + health).
- **Deploy**: template.yaml SAM válido (Lambda 256MB / 15s + HTTP API
  + CORS localhost:3000). **Sin samconfig.toml**, **sin GH Actions**.
  Deploy es interactivo (`sam deploy --guided`).
- **BLOQUEADOR CRÍTICO**: no hay migraciones SQL. Sin
  `scripts/init-db.sql` o Prisma migrate, el backend no arranca contra
  DB vacía. El código asume tablas pre-creadas.
- **Otros faltantes**: jobs de cierre mensual documentados en
  `docs/jobs-y-alertas.md` pero no implementados. RBAC documentado pero
  endpoints solo validan presencia de user, no rol. Sin tests.

### Parent (`BudgetApp`) y mobile

- Parent: solo docs/coord. README dice `lerna-admin/...` cuando ya
  están en `lerna-soft` — desactualizado, fix trivial pendiente.
- Mobile: no diagnosticado a fondo por instrucción explícita del user
  ("primero el frontend, luego mobile"). Último commit real
  2026-04-25 "form sencillo de creación de presupuesto".

### Caminos propuestos al user

A) **Front estricto** — pulir UI, limpiar `pg`/`nodemailer` del front,
   tests críticos del wizard, deploy a Vercel apuntando a mock o localhost.
   Riesgo: no es publicable end-to-end sin tocar back.

B) **Back primero** — generar migraciones SQL, `samconfig.toml`, deploy
   AWS (o pivotar a Render/Railway con Docker si SAM es overkill), GH
   Action. Luego volver al front limpio.

C) **Paralelo** — preparar deploy del back (migraciones + samconfig +
   action) mientras se limpia el front. Lo más rápido al MVP publicable.

Recomendé C. Esperando decisión del user.

## Decisión: trabajamos solo local

User aclaró: "trabajar por ahora local, no necesito publicar nada en
internet aun, solo en github y no de forma publica". Esto descarta
Vercel/AWS por ahora. **Stack objetivo: localhost:3000 (front) +
localhost:3001 (back) + Postgres local.** Todo en repos privados de
`lerna-soft`.

Esto cambia el orden: ya no hay que preocuparse por deploy aún. El
bloqueador real ahora es **levantar la DB local + tablas creadas**
para que el back arranque y se pueda probar el front contra él.

## Inventario de features del frontend (auditoría 2026-05-27)

User pidió la lista clara de qué está listo, incompleto y falta.
Auditoría exhaustiva del frontend.

### Menú del sidebar (`src/components/dashboard-sidebar.jsx`)

**Sección Principal**: Dashboard `/`, Presupuesto `/presupuesto`,
Registro de movimientos `/gastos`, Metas `#` (no implementada).

**Sección Herramientas**: Dividir cuentas `/herramientas/dividir-cuentas`.

**Sección Config**: Bancos `/bancos`, Deudas `/deudas`, Reglas `#`,
Alertas `#`, Importar `#`, Tema `#` (existe en /profile no separado).

**Sección Cuenta**: Perfil `/profile`, Reportes `#`.

### ✅ Features listas para probar local (8)

1. Auth completo (login, registro, forgot-password, reset-password).
2. Dashboard con gráficos (línea + pie), cards de resumen, lee de
   `/api/expenses`, `/api/financial-reality/summary`, `/api/budgets/current`.
3. Presupuesto wizard 5 pasos, perfiles predefinidos
   (Tilbury, Ahorro Acelerado…), varianzas plan-vs-real por periodo
   YYYY-MM. `budget-wizard.jsx` 1813 líneas.
4. Movimientos/Gastos: CRUD completo + filtros + búsqueda + tags +
   detección de duplicados. `expense-register.jsx` 1552 líneas.
5. Bancos: Tabs Cuentas/Tarjetas, multi-moneda COP/USD/EUR, CRUD.
6. Deudas: CRUD + cálculo interés EA + pago mínimo. 628 líneas.
7. Dividir cuentas: eventos + participantes + ítems + cálculo
   automático de cuota. 5108 líneas (la feature más grande).
8. Perfil: card + acordeones (Seguridad, Preferencias, Cuentas,
   Tarjetas), tema en localStorage.

### ⚠️ Features parciales (1)

- `/gastos`: botones "Exportar" e "Importar movimientos" en
  `src/app/gastos/page.jsx:31-32` apuntan a `#`. UI presente, sin handler.

### ❌ Features prometidas en el menú pero NO implementadas (6)

- **Metas** (Principal) → no existe `src/app/metas/`.
- **Reglas** (Config).
- **Alertas** (Config).
- **Importar** (Config).
- **Tema** (Config) — funciona dentro de /profile pero no como página.
- **Reportes** (Cuenta).

Todas como `href="#"` en `dashboard-sidebar.jsx`.

### Calidad del código

Cero `TODO`/`FIXME`/`XXX`/`HACK`. Cero `console.log` colgados. Cero
`alert()` (todo migrado a SweetAlert2). Cero datos mockeados en las
features listas. 29 rutas proxy en `src/app/api/` cubren todos los
endpoints consumidos. Sin directorios huérfanos en `src/app/`.

### Plan propuesto al user

1. **Levantar el stack local primero** (DB + back + front). Sin esto
   no se prueba nada de lo ✅ listo. Pasos: generar `init-db.sql` con
   el esquema que el back asume, arrancar Postgres, configurar
   `.env.local` del back, correr `local-server.js`, configurar
   `.env.local` del front apuntando a `localhost:3001`, `npm run dev`
   del front.
2. Después ir cerrando las ❌ una por una (Metas → Reportes →
   Alertas → Reglas → Importar/Exportar). Confirmación pendiente.

## Stack local arriba (2026-05-27, after-noon)

User: "empieza a trabajar en lo que esta mal y pendiente tal como me
lo ofreces". Procedí sin más preguntas, en pasos pequeños.

Hitos:

1. **Esquema SQL extraído** del código del backend
   (`src/lib/server/*-repository.js`) — 16 tablas. Generado
   `scripts/init-db.sql` idempotente con CREATE TABLE IF NOT EXISTS,
   índices y seed de países (CO, US, MX, ES, AR).
2. **Postgres 16 instalado** (apt — solo había cliente, ni server ni
   Docker). Servicio arriba, conexión OK.
3. **Rol + DB** creados: rol `budgetapp` con password `budgetapp`,
   DB `budgetapp_dev`. Esquema aplicado, 16 tablas OK.
4. **Backend** levantado con `npm run dev` en `localhost:3001`. Lee
   `env.local.json`. `/health` → 200.
5. **Frontend** levantado con `npm run dev` en `localhost:3002`
   (el 3000 está ocupado por un nginx del sistema). CORS del back
   acepta cualquier localhost, no requirió cambios. Solo ajusté
   `APP_BASE_URL` en back y front a `:3002` para los links de
   password reset.
6. **Smoke test end-to-end exitoso** vía proxy del front:
   - `POST /api/auth/register` → 201, user `henry@budgetapp.local`
     creado (id `2d5fccb5-8909-441e-9a3d-4141c6020aa3`).
   - `POST /api/auth/login` → 200, cookie de sesión emitida.
   - `GET /api/auth/me` con cookie → 200.
   - `POST /api/expenses` → 201, expense `b7d5e1d3-a35b-...` creado.
   - `GET /api/expenses?limit=5` → 200, lista al expense.

### Commit en `budgetapp-backend`

`bc4e94f` Add init-db.sql with full schema for local dev. Push OK.

### Credenciales de dev

- DB: `postgresql://budgetapp:budgetapp@localhost:5432/budgetapp_dev`
- User de prueba: `henry@budgetapp.local` / `test1234`
- Front: http://localhost:3002
- Back: http://localhost:3001

### Tasks creadas para el camino al MVP

#1-5 cierran levantamiento + smoke (✅ done).
#6 limpiar pg/nodemailer del front (probablemente residuo).
#7 implementar Export/Import de gastos (los botones a `#`).
#8 implementar página /metas (back ya tiene `/savings-goals`).
#9 implementar página /reportes.
#10 implementar /alertas + /reglas + /importar (las 3 restantes).

## Features cerradas en este turno

### Export/Import de gastos (cierra task #7)

Commit `a3499b1` en budgetapp-frontend:
`src/components/gastos-actions.jsx` (Client Component) +
`src/app/gastos/page.jsx` (reemplaza los `<a href="#">`).

- **Exportar**: GET `/api/expenses?limit=2000` → CSV con 15 columnas
  estándar → download via Blob URL. Maneja comillas y comas. Tags
  con separador `|` para no chocar con la coma del CSV.
- **Importar**: file picker → parse CSV → normaliza filas → valida
  (movementType, method, amount > 0, currency) → muestra preview
  con conteos válidos/inválidos vía Swal → POST por cada fila →
  reporta éxito/parcial vía SweetAlert2 → refresh.

### Página /metas (cierra task #8)

Commit `69df1f4` en budgetapp-frontend:
- `src/app/metas/page.jsx` (Server Component, mismo patrón que /deudas).
- `src/components/savings-goals-manager.jsx` (Client Component) — CRUD
  completo con summary strip, form, tabla con progress bar, y
  proyección de tiempo de logro (basada en cuota mensual o fecha
  objetivo).
- Sidebar: la entrada "Metas" apunta a `/metas` en vez de `#`.

Verificado end-to-end: `GET /metas` 200, `POST /api/savings-goals`
201 (creó "Vacaciones 2026", objetivo COP 3M, acumulado 150K,
cuota 300K, fecha 2026-12-31).

## Decisión sobre task #6

Diferida. `pg`/`nodemailer`/`dotenv` están en uso por
`scripts/migrate.js` + 16 archivos en `migrations/`. No es residuo
sino un sistema paralelo de migraciones del front. Cleanup correcto
implica mover `migrations/` al back, no solo borrar las deps. No
bloquea el MVP local. Lo retomo después de cerrar las features ❌
restantes.

## Página /reportes cerrada (task #9)

Commit `fe795b5` en budgetapp-frontend.

- `src/app/reportes/page.jsx` + `reports-manager.jsx`.
- Selector de periodo (este mes / mes pasado / últimos 3 meses /
  este año / todo) + filtro de moneda (auto-detectado).
- Summary strip: ingresos, gastos, ahorro, inversión, **balance neto**.
- Tabla "Gastos por categoría" con barra de distribución relativa.
- Top 10 gastos del periodo.
- Como el backend no expone filtros server-side por fecha hoy, el
  filtrado se hace cliente-side trayendo `/api/expenses?limit=2000`.
  Funciona para MVP; escalable a server-side cuando crezca.

## /alertas y /importar cerradas; /reglas diferida (task #10)

Commit `f2f6768` en budgetapp-frontend.

### /alertas (deriva en cliente)
- `alerts-manager.jsx` lee `/api/debts`, `/api/savings-goals`,
  `/api/expenses?limit=2000` y produce 5 tipos de alertas:
  - Deudas vencidas (severity critical) o próximas a vencer (≤7
    días, warning).
  - Metas con fecha objetivo en ≤30 días y progreso <80%.
  - Gasto del mes > 1.5x promedio histórico mensual.
  - Categorías con gasto > 2x promedio histórico mensual.
  - Ausencia de movimientos en ≥14 días.
- Sin endpoints nuevos: todo es derivado de datos existentes.

### /importar (hub)
- Cards apuntando a las herramientas reales: `/gastos` (CSV
  in/out real), `/bancos` y `/presupuesto` (placeholders honestos
  "próximamente").
- Documenta el formato CSV de movimientos.

### /reglas diferida
- Requiere modelo backend nuevo (tabla `rules` + handler en
  `POST /expenses` para auto-categorización). Solo UI no tiene valor
  sin el backend que las aplique. Queda en el sidebar con
  `href="#"` hasta que se priorice el backend de reglas.

## Mejora UX en wizard de presupuesto (nuevo task #11, cerrado)

User reportó mientras yo trabajaba: "cuando seleccionas los perfiles,
debe indicar qué trata el perfil, qué distribución/manejo en
porcentajes de los ingresos recomienda. Importante para que el
usuario pueda decidir con datos. Así como está no es claro."

Commit `ff9c1dc` en budgetapp-frontend.

- Reemplazado el `<select>` plano del paso 3 por un **grid de
  tarjetas seleccionables** (4 perfiles: Tilbury, Ahorro Acelerado,
  Inversión Prioritaria, Pago de Deudas).
- Cada tarjeta ahora muestra:
  - **Headline** corta (filosofía del perfil).
  - **Ideal: ...** (público objetivo) para que el user identifique
    si aplica a su situación.
  - **Distribución completa** de los 4 buckets con % numérico +
    barra mini de progreso.
- Estado seleccionado destacado con borde teal y label
  "SELECCIONADO".
- La sección de Distribución debajo ahora también tiene descripción
  larga del perfil, barras grandes en cada fila y total acumulado
  de % (para detectar si suma 100%).

## Resumen de este sub-turno

8 commits productivos en `budgetapp-frontend`:

1. `a3499b1` Wire Export/Import buttons in /gastos.
2. `69df1f4` Add /metas page for savings goals CRUD.
3. `fe795b5` Add /reportes page with period-based consolidated view.
4. `f2f6768` Add /alertas and /importar; defer /reglas.
5. `ff9c1dc` Show profile distribution and rationale in budget wizard.

Sidebar antes: 6 entradas con `href="#"` (Metas, Reglas, Alertas,
Importar, Tema, Reportes).
Sidebar ahora: solo 2 con `href="#"` (**Reglas** diferida con
justificación; **Tema** funciona dentro de /profile).

Stack local sigue arriba (back :3001, front :3002, postgres :5432).

### Pendientes ya documentados para próxima sesión

- Task #6: limpiar `pg`/`nodemailer`/`dotenv` del front migrando
  `migrations/` al back. Requiere decisión de modelo.
- Task #10 (reglas diferidas): diseñar backend para reglas de
  auto-categorización (tabla `rules` + aplicación en POST
  `/expenses`).
- /presupuesto verificación manual: el user va a revisar las
  tarjetas de perfil; recibir feedback antes de tocar más el wizard.

## Hotfix UX: perfiles en /register también (task #12)

User entró a la app y no veía los cambios — porque estaba en
`/register`, no en el wizard de `/presupuesto`. La página de
registro tenía su propio bloque de perfiles separado (caja "Alerta
de lectura" con purpose/idealFor pero sin distribución de %).

Commit `0a6acc3` en budgetapp-frontend.

- Agregada propiedad `distribution` a cada perfil de
  `src/app/register/page.jsx` (4 buckets cada uno).
- Reemplazado el `<select>` por el mismo grid de tarjetas
  seleccionables del wizard, con barras de % y estado teal.
- La caja "Alerta de lectura" preservada como "Sobre tu perfil
  seleccionado" debajo del grid.

### Hallazgo importante para próxima sesión

Hay **dos definiciones distintas de los perfiles**, una en
`/register` (`mark-tilbury`, `savings-boost`, ...) y otra en
`/presupuesto` (`tilbury`, `ahorro`, ...). IDs distintos, %
similares. Si el user crea cuenta con un perfil del registro, el
wizard no lo va a cargar con esa misma distribución.

Además, los % de "Tilbury" en el código (50/25/15/10) NO coinciden
con la **regla del 1% pública** de Mark Tilbury (que él enseña
como 75/15/10 en sus videos). El user específicamente lo mencionó:
"Tilbury en sus videos hablas de una division por porcentajes
interesante eso no lo veo aqui".

Próximo paso pendiente: confirmar con el user qué set de
distribuciones quiere para Tilbury y consolidar a un solo archivo
compartido `src/lib/budget-profiles.js` entre `/register` y el
wizard.

## Definiciones de buckets (claridad de vocabulario)

User: "me gusta aunque en la parte que habla del perfil debería decir
a qué se refiere cada item en que se está dividiendo por que no es
claro lo que es recompensa".

Commit `6bf3768` en budgetapp-frontend.

- Nuevo módulo compartido `src/lib/budget-buckets.js` con definición
  `short`, `long` y `examples` para los 5 buckets: **Esenciales,
  Crecimiento / Inversión, Ahorro / Estabilidad, Deuda, Recompensas**.
- `/register` ("Sobre tu perfil seleccionado"): bajo la caja, lista
  cada bucket del perfil con descripción larga + ejemplos concretos.
- `/presupuesto` wizard paso 3: misma sección "¿Qué significa cada
  bucket?" debajo del bloque de distribución editable.

Lección guardada: **claridad de vocabulario** es importante para
este user — no asumir que entiende términos del dominio financiero.

## Detalles del perfil colapsables

User: "donde muestra los detalles del perfil seleccionado debería
poder colapsarse o expandirse".

Commit `2cfeaf7` en budgetapp-frontend.

Usé `<details>`/`<summary>` HTML nativos (sin state extra,
accesibles por teclado y screen reader). Por defecto **cerrados**
para no saturar la vista; el user hace clic si quiere ver detalles.

- `/register`: la caja "Sobre tu perfil seleccionado" arranca
  cerrada con hint "Haz clic para ver detalles...".
- `/presupuesto` wizard paso 3: misma idea en "¿Qué significa cada
  bucket?".

## Rescate del main + split layout en rama nueva

User: "no quiero que trabajes sobre el main, si lo estas haciendo
deberias deshacer todos los cambiods que has hecho o giardarlos en
una rama y devolver el main al punto donde estaba antes de que
empezaras a tocarlo".

Yo había pusheado 8 commits a `main` de `budgetapp-frontend` y 1 a
`budgetapp-backend`. Rescate ejecutado:

- `budgetapp-frontend`: creada rama `feat/local-mvp-iteration`
  apuntando a `2cfeaf7` (todo mi trabajo), pushed. Reset hard de
  main a `cbff25a` ("Add nested documentation index"), force push.
- `budgetapp-backend`: creada rama `feat/local-mvp-init` apuntando
  a `bc4e94f`, pushed. Reset hard de main a `2c4bd8e`, force push.

Memoria guardada: `feedback-branch-workflow.md` para que en futuras
sesiones no vuelva a pushear a main.

### Layout 2-col en /register (rama nueva)

User pidió: "el registro deberia usar un tema de 2 visuales es
decir el formulario cabe a la izquierda o derecha y los detalles
los podemos ver al otro lado, revisa opciones en linea para basarse
y luego haz el cambio me dices cuando este solamente listo para
aprobar no me pidas autorizaciones extras".

Rama: `feat/register-split-layout` (desde
`feat/local-mvp-iteration`). Commit `de47559`.

WebSearch confirmó el patrón split común en auth pages (Stripe,
Vercel, Clerk). Caveat documentado: campos del form deben quedar
en **una sola columna**; el split es entre form-side vs
content-side. Mobile debe colapsar a 1 columna.

Aproveché `.auth-layout` (grid 1.04fr/0.96fr) y `.auth-aside`
que ya existían en `globals.css` sin uso, con media query
responsive ≤900px → 1 columna.

Resultado:
- **Columna izquierda** (`.auth-main`): formulario completo
  (nombre/email/password/país/grid de perfiles/términos/submit).
- **Columna derecha** (`.auth-aside`): "Vista previa del perfil
  seleccionado" en vivo. Sin colapsar (tiene espacio dedicado).
  Muestra: nombre + purpose + idealFor + distribución con barras
  + "¿Qué significa cada bucket?" con descripción larga + ejemplos.
- En mobile colapsa: form arriba, preview debajo.

Listo para aprobación visual del user.

### Iteración: país auto-detectado en /register

User: "no podrias calcular el pais por el navegador? es que ese
campo me estorba".

Commit `6aa0d72` en la misma rama `feat/register-split-layout`.

- `detectCountryFromBrowser()` lee `navigator.languages` /
  `navigator.language`, extrae la región (ej. "es-CO" → "CO").
- Si no encuentra match en {CO, MX, BR}, fallback por timezone
  (`Intl.DateTimeFormat().resolvedOptions().timeZone`):
  Bogota → CO, Mexico → MX, Sao_Paulo/BuenosAires → BR.
- Default final: CO.
- `useEffect` en mount setea el país y marca `countryAutoDetected`.
- El campo ahora muestra solo una línea compacta: "País detectado:
  Colombia (COP) · Cambiar". El `<select>` aparece únicamente si
  el user hace clic en "Cambiar".

### Iteración: compactar /register, logo al aside

User: "no me gusta aun hay cosas por mejorar mira como quedo la
distribucion en desktop por ejemplo no veo todo de una me toca
hacer scroll, tal vez el logo deberia estar en otra parte no el
lado del formulario".

Commit `68e8bc7` en `feat/register-split-layout`.

- **Logo movido al aside**: ya no aparece arriba del form sino
  arriba del panel derecho (con "Registro de usuario" como
  sub-título). El form arranca directo con "Crear cuenta".
- **Form más denso**: padding 22 (era 28), h2 22px (era 28).
- **Tarjetas de perfiles muy compactas**: solo nombre + purpose
  corto + checkmark si seleccionado. Las barras de distribución
  salieron de las tarjetas — viven solo en el aside, evitando
  duplicar info.
- **Aside sticky**: `position: sticky, top: 16,
  max-height: calc(100vh - 32px), overflow-y: auto`. Queda visible
  mientras el user scrollea el form y tiene su propio scroll
  interno si el contenido excede.
- Font sizes del aside reducidos a 10–12 para que entren
  Distribución + Definiciones de buckets sin saturar.

### Iteración: header full-width arriba de las 2 cols

User: "buscar una forma de reorganizar pero dividiste eso en 2
columnas para este caso creo que deberias tener un header donde
este el logo y abajo las 2 columnas".

Commit `1a6ac67` en `feat/register-split-layout`.

Estructura nueva:

```
┌────────────────────────────────────────────┐
│ HEADER: [Logo BudgetApp]      Inicia sesión│
├────────────────────┬───────────────────────┤
│ FORM (auth-main)   │ ASIDE (auth-aside)    │
│ Crear cuenta       │ Vista previa del      │
│ ...                │ perfil + dist + buckets│
└────────────────────┴───────────────────────┘
```

- El logo salió del aside, ahora vive en un header full-width
  arriba del `.auth-layout`.
- A la derecha del header agregué un atajo "Inicia sesión →" a
  /login para usuarios existentes (UX común en signups).
- Aside `max-height` ajustada a `calc(100vh - 110px)` para
  considerar la altura del header sobre el viewport.

### Iteración: márgenes laterales reducidos

User: "esta mejor pero creo que deberias reducir en el registro el
margen que esta a la izq y der".

Commit `d6a8dc6` en `feat/register-split-layout`.

Wrapper width pasó de `min(1160px, 100%)` a
`min(1480px, calc(100% - 24px))`. En desktop ocupa hasta 1480px
(antes 1160, ~28% más ancho), en mobile mantiene 12px de margen
mínimo por lado.

### Iteración: columnas 50/50 reales

User: "las columnas no las cambiaste deben ocupar lo mismo por
mitades en lo disponible".

Commit `1807f62` en `feat/register-split-layout`.

En `globals.css`:
- `.auth-layout` grid-template-columns: `1.04fr 0.96fr` →
  **`1fr 1fr`** (50/50 exacto).
- `width: min(1160px, 100%)` → `width: 100%` porque el ancho ya
  lo controla el wrapper externo `min(1480px, calc(100%-24px))`.

La media query ≤900px sigue colapsando a 1fr (single col en mobile).

### Iteración: header más ajustado

User: "reduce un poco el margen que tiene el navbar o header tanto
arriba como abajo".

Commit `c509d43` en `feat/register-split-layout`.

- Header padding vertical: `8px` → `0` (mantiene `12px` horizontal).
- Gap entre header y `.auth-layout`: `14` → `8`.

### Iteración: form sin scroll + menos margen superior

User: "reduce un poco mas el margen superior, ademas el lado
izquierdo donde esta el formulario de registro no cabe todo en la
visual sin scroll, podemos ajustarlo de alguna manera?".

Commit `f905b61` en `feat/register-split-layout`.

- **Margen superior**: wrapper externo con `alignSelf: start` +
  `marginTop: 12`. Neutraliza el centrado vertical del `.auth-shell`
  solo para `/register` (sin afectar `/login` ni `/forgot-password`).
- **Padding del form panel**: 22 → 16.
- **h2 "Crear cuenta"**: 22px → 18px, marginBottom 14 → 10.
- **Tarjetas de perfil**: grid auto-fit minmax 220 → **1fr 1fr fijo**
  (2x2 siempre en desktop, gap 10 → 8). Antes, en pantallas medianas
  podían quedar 1 col cada una ocupando mucho vertical.
- Quitada la hint expandida "Elige una filosofía…"; el mensaje vive
  ahora en el tooltip del info-dot del label.
- CSS scoped en `globals.css`:
  - `.auth-main .form-field { margin-bottom: 8 }` (era 12 global).
  - `.auth-main .password-rules { margin-top: 4; font-size: 10 }`
    (era 8 y default).

### Iteración: aún más compacto

User: "aun no cabe veo campo de accion en el header o navbar".

Verificado: `AppShell` no agrega navbar global, solo es un wrapper
`<main>`. El "header/navbar" que ve es el que agregué (logo +
"Inicia sesión →").

Commit `17d2e3b` en `feat/register-split-layout`.

- **Nombre + Email**: ahora en una sola fila (grid 1fr/1fr).
  Reduce 1 form-field de altura.
- **Tarjetas de perfil**: solo nombre + checkmark si seleccionado.
  Sacado el párrafo `purpose` (que igual aparece en el aside cuando
  el perfil está activo).
- **Quitado el footer del form**: "Ya tienes cuenta? Inicia sesión"
  era duplicado del link "Inicia sesión →" que ya vive a la derecha
  del header.

### Iteración: "Vista previa del perfil" como nota top-right

User: "el titulo vista previa de perfil no deberia ocupar espacio
deberia estar pequeño como nota al lado derecho superior de esa
columna".

Commit `84c2dbe` en `feat/register-split-layout`.

- "Vista previa del perfil" pasa de label uppercase encima del
  nombre (consumía una línea entera) a una nota chiquita
  (font-size 9, gris) en la esquina superior derecha del primer
  bloque del aside, alineada con el `<h3>` del nombre vía flex
  `justify-between`.
- El nombre del perfil (h3) ahora es el título principal arriba
  a la izquierda.

### Iteración: causa raíz del scroll — padding del shell + tamaño del logo

User: "puedes revisar por que aun no cabe tengo scroll y deberia
caber todo".

Audité CSS y encontré dos culpables:

1. `.page-shell` (clase base del `<main>` del AppShell) tenía
   `padding: clamp(14px, 2.8vw, 28px)` → hasta 28px arriba y 28
   abajo = 56px solo de padding del shell.
2. `.brand-logo` mide `clamp(70px, 5.5vw, 80px)` — el logo del
   header pesaba hasta 80x80px, sumando ~90px de alto al header.

Commit `5bd893a` en `feat/register-split-layout`. Reglas scoped
solo a `.auth-shell` para no afectar otras páginas:

- `.page-shell.auth-shell { padding: 10px clamp(14px, 2.8vw, 28px) }`
  → padding vertical fijo a 10px (era hasta 28).
- `.auth-shell .brand-logo { width: 36px; height: 36px }` (era 70-80).
- `.auth-shell .brand-name { font-size: 14px }`.
- `.auth-shell .brand-sub { font-size: 11px }`.

Solo afecta `/register`, `/login`, `/forgot-password`, `/reset-password`
(las cuatro usan `.auth-shell` via AppShell className). `/presupuesto`
y dashboard no se ven afectadas.

### Onboarding A: página /bienvenida después del registro (task #13)

User: "ahora digamos que creo la cuenta, qué debe pasar a
continuación?" → propuse dos opciones, el user eligió A.

**Backend** — rama `feat/welcome-onboarding` en `budgetapp-backend`.
Commit `b02fed8`.

- Nueva columna `users.profile_template TEXT` (nullable).
- `init-db.sql` incluye la columna en `CREATE TABLE` + `ALTER TABLE
  IF NOT EXISTS` para DBs ya creadas.
- `users-repository.js`: `mapUserRow` expone `profileTemplate`;
  `createUser` lo INSERTea; queries `SELECT` la incluyen.
- `routes/auth.js`: `register` lee `body.profileTemplate` y lo pasa;
  `sanitizeUser` lo expone en `/auth/me` y respuesta del registro.
- Aplicado `ALTER TABLE` a la DB local. Smoke test: nuevo user
  `onboard@budgetapp.local` registrado con `profileTemplate:
  "savings-boost"` y persistido.

Antes: el frontend mandaba `profileTemplate` y el backend lo
descartaba silenciosamente. Bug latente desde la primera versión.

**Frontend** — rama `feat/register-split-layout` en
`budgetapp-frontend`. Commit `8a797c0`.

- `src/lib/profile-templates.js` (nuevo): `PROFILE_TEMPLATES` array
  centralizado + `DEFAULT_PROFILE_TEMPLATE` + helper
  `getProfileTemplate(value)`. Reemplaza las 4 definiciones que
  vivían inline en `register/page.jsx`.
- `src/app/bienvenida/page.jsx` (Server Component):
  - Si no hay sesión → redirect `/login?next=/bienvenida`.
  - Saludo "Hola, {firstName}. Bienvenido."
  - Card con el perfil elegido (leído de `user.profileTemplate`),
    distribución con barras y descripción corta de cada bucket
    (vía `getBucketInfo`).
  - 3 cards-CTA: "Agregar primera cuenta" (`/bancos`), "Crear primer
    presupuesto" (`/presupuesto`, destacada en teal con borde
    grueso), "Ir directo al dashboard" (`/`).
  - Header con logo izquierda + "Saltar al dashboard →" derecha
    como escape rápido.
- `/register` ahora redirige a `/bienvenida` post-submit en lugar
  de `/`.
- `/register` usa el módulo compartido para los perfiles.

Verificación end-to-end: registro vía proxy con
`profileTemplate: "savings-boost"` → user creado → cookie de sesión
→ `/api/auth/me` devuelve el campo → `/bienvenida` carga 200 con
el perfil correcto.

### Realidad fuera del wizard, como gate previo

User: "la realidad es importante para el presupuesto o no? si no
lo es deberias ofrecerla pero antes del presupuesto tambien por
que no tiene sentido, dale la opcion de saltar y luego si el
usuario quiere puede ir llenando las secciones a mano desde el
menu".

Commit `0b456f2` en `feat/register-split-layout`.

Diagnóstico previo: el wizard ya tenía el botón "skip" en el step 1,
pero estaba **dentro del wizard**, lo que daba la impresión de que
configurar la realidad era parte del flujo de armar el presupuesto.
Confuso.

Cambios:

- `budget-wizard.jsx`: `visibleSteps` ahora SIEMPRE filtra
  `step.id !== 1`. El wizard arranca en "Periodo y nombre" (id=2).
  El JSX del step 1 queda como dead path (no se renderiza). Limpieza
  total del código del step 1 en iteración posterior si se quiere.

- `/presupuesto/page.jsx`: gate previo al wizard. Cuando
  `needsRealitySetup === true`, no viene `?reality=skip` y no hay
  presupuesto del periodo, se muestra un panel con dos cards:
  - **Recomendado** (borde teal): "Configurar mi realidad" → `/bancos`.
    Desde ahí el user puede ir a Deudas, Metas, etc. desde el menú.
  - "Saltar y armar presupuesto" → misma página con `?reality=skip`,
    lo que desbloquea el wizard.

- Cuando se renderiza el wizard, se le pasa
  `needsRealitySetup: false` explícitamente para que no intente
  abrir el step 1.

**Visual del wizard**: el user también pidió mejorarla. Pendiente
de feedback puntual (qué específicamente le ve mal). Creada
task #14 para retomar.

### Limpieza de redundancia "sin presupuesto" en /presupuesto

User: "si tengo la opcion de saltar y armar presupuesto por que
tengo un boton de crear nuevo en la parte superior eso es
innecesario y lo otro es que si no tengo presupuestos pues ya lo
se deberia simplemente no mostrar y ya".

Commit `5f1cb6e` en `feat/register-split-layout`.

Cuando no hay presupuesto del periodo, escondo:
- Botón "Recalcular" del topbar (no hay nada que recalcular).
- Botón "Ir al wizard" del topbar (el wizard ya está abajo).
- Toda la sección "Sin presupuesto en X" con "Crear nuevo", "Iniciar
  desde cero", "Usar base…" — redundante con el gate de realidad
  o el wizard.
- Card `budget-intro` (nombre+estado+chips) — no tiene sentido para
  presupuesto inexistente.
- Placeholder "Aún no tienes presupuestos guardados" — si no hay,
  no se renderiza la sección.

Único elemento útil que se conserva cuando `!hasBudget`:
- Si hay un periodo anterior, el topbar muestra "Usar base {mes}" para
  arrancar el wizard desde una plantilla.
- Si `isUsingTemplate`, una línea hint pequeña con el periodo base.

Resultado: la página entra directamente al gate de realidad (o al
wizard si ya se saltó la realidad), sin distractores.

### Wizard guiado de configurar realidad

User: "el tema de configurar la realidad debe ser claro en el wizard
si selecciono esa opcion debe ayudarme y guiarme en la creacion de
todo sin entrar a las vistas directamente".

Antes el botón "Configurar mi realidad" del gate de /presupuesto
sacaba al user a `/bancos` y lo dejaba navegar por su cuenta al
resto. Confuso.

Commit `ed3c747` en `feat/register-split-layout`.

**Nueva ruta `/configurar-realidad`** con wizard guiado de 4 pasos:

1. **Cuentas y efectivo** — form inline (nombre, tipo, moneda,
   saldo) + tabla con eliminar.
2. **Deudas activas** — opcional, skipeable.
3. **Metas de ahorro** — opcional, skipeable.
4. **Pagos fijos** — opcional, skipeable.

Componente `src/components/reality-setup-wizard.jsx` (client):
- Carga listas existentes en `useEffect` con `Promise.all` GET
  `/api/{accounts,debts,savings-goals,recurring-bills}`.
- Cada paso: form mini → POST → push al state.
- Tabla con DELETE inline (con confirmación destructiva).
- Nav superior con chips por paso (icon + label, activo en teal,
  completados con check verde).
- Contador en esquina sup. derecha con totales por sección
  (🏦 N, 💳 N, 🎯 N, 📅 N).
- Botones: Anterior, "Cancelar e ir al presupuesto"
  (`?reality=skip#wizard`), "Saltar paso" / "Terminar e ir al
  presupuesto" en el último.

Server component `/configurar-realidad/page.jsx` usa el shell del
dashboard (sidebar + topbar) para consistencia, con
`activeItem="budget"`.

Gate de `/presupuesto`: el botón "Configurar mi realidad" ahora
apunta a `/configurar-realidad` y la copy explica el wizard de 4
pasos.

Al terminar el wizard redirige a `/presupuesto#wizard`. Como ya
hay realidad, el gate ya no se muestra y el budget wizard arranca
directo en "Periodo".

### Botón "Saltar paso" → "Siguiente" cuando hay items

User: "no deberia decir saltar paso deberia decir siguiente, si ya
cree algun item".

Commit `ac7661d` en `feat/register-split-layout`.

En `reality-setup-wizard.jsx`, el botón principal del footer cambia
de label según el contenido del paso actual:
- Items > 0 en el paso → "Siguiente →" (el user avanza con datos).
- Items == 0 → "Saltar paso →" (el user salta el paso vacío).

Lectura del conteo se hace mirando `accounts`/`debts`/`goals`/`bills`
según `step.id`.

### Selector de periodo en topbar de /presupuesto

User: "estoy a 27 de mes, voy a crear un presupuesto ya no tiene
sentido hacerlo con el mes en curso pero al darle editar no me
deja editar el mes y año del presupuesto que quiero crear".

Causa raíz: el step "Periodo y nombre" del wizard se oculta cuando
hay `hasInitialBudget` (los visibleSteps filtran a 4 y 5 para no
romper la integridad del presupuesto existente). Sin un selector
externo de periodo, no había forma de saltar a otro mes.

Commit `8fe3386` en `feat/register-split-layout`.

- Nuevo `src/components/period-picker.jsx` (Client Component): dos
  `<select>` (mes + año ±5 años) en el topbar de `/presupuesto`.
- onChange hace `router.push("/presupuesto?period=YYYY-MM#wizard")`.
- Disponible siempre (hayBudget o no), en cualquier mes.

Flujo arreglado:
1. Estoy en mayo y ya armé el presupuesto de mayo.
2. Cambio el dropdown de mes a Junio → URL cambia a
   `?period=2026-06`.
3. Si Junio no tiene presupuesto, aparece el gate de realidad o el
   wizard nuevo.
4. Si Junio tuviera presupuesto, vería el card de intro + botones.

### Alertas plan-vs-perfil en el wizard de presupuesto

User: "segun el perfil seleccionado, si el presupuesto base indica
sobrepasa alguna de las secciones deberia decirlo desde la creacion
de presupuesto para ajustarse con tiempo".

Commit `b3c13af` en `feat/register-split-layout`.

En `budget-wizard.jsx`, step 4 (Valores planificados), debajo del
balance-meter:

- Tabla nueva "Plan vs perfil" que agrupa los rubros del plan en los
  5 buckets posibles (Esenciales, Crecimiento/Inversión, Ahorro/
  Estabilidad, Deuda, Recompensas) usando keywords en el nombre del
  rubro.
- Para cada bucket: % sugerido y monto sugerido (basado en ingreso
  total × % del perfil), % y monto planificado, diferencia, status.
- Status:
  - **over** (rojo): plan > sugerido +$1.
  - **under** (gris): hay margen disponible.
  - **ok** (teal): coincide con el sugerido.
  - **extra** (ámbar): bucket que no está en el perfil (categoría
    sin clasificar por keywords).
- Header con badge: "N bucket(s) sobrepasa(n) el perfil" en rojo o
  "Dentro del perfil" en teal.
- Pie con sugerencias: ajustar rubros, cambiar perfil en paso 3, o
  aceptar la diferencia.

Helper `bucketFromName(name)`: keywords por bucket (necesidad/basic/
esencial/aliment/transport… → Esenciales; ahorro/emergencia/fondo →
Ahorro; etc.).

Cálculo reactivo (useMemo): cambia en vivo al editar montos.

### Inputs de monto con separadores de miles en vivo

User: "puede de una vez escribir e ir colocando los puntos de miles
millones y demas es que me ayudaria a meter bien el dedo".

Commit `46265ff` en `feat/register-split-layout`.

- Nuevo `src/components/money-input.jsx`: input `type="text"` con
  `inputMode="numeric"`. Muestra el valor con puntos de miles
  (`1.500.000`) y emite `onChange(raw)` con dígitos crudos
  (`1500000`). Soporta `readOnly` y `disabled`.
- En `budget-wizard.jsx`, step 4 (Valores planificados), reemplazo
  6 inputs de monto por `<MoneyInput>`:
  - Ingreso por fila.
  - Recurring bills amount (en reality step inline).
  - Expenses subcategory amount (readOnly cuando tiene detalles).
  - Expenses detail amount.
  - Savings subcategory amount (readOnly cuando tiene detalles).
  - Savings detail amount.
- `parseAmount()` del wizard ya limpia puntos, así que serialización
  al backend no cambia: state guarda "1500000", UI muestra
  "1.500.000".

Pendiente replicar el patrón en `/configurar-realidad`, `/deudas`,
`/metas`, `/gastos` si el user confirma que le gusta.

### Fallback de buckets faltantes a Esenciales (opción Tilbury)

User eligió la opción 2 de mi respuesta sobre Tilbury: "quedarte
con Tilbury y meter los pagos de deuda dentro de Esenciales para
que la tabla plan-vs-perfil alerte cuando Esenciales se pase".

Commit `a0a27b4` en `feat/register-split-layout`.

En el `bucketAnalysis` del wizard:
- Cuando un bucket detectado en el plan (ej. "Deuda") no existe en
  el perfil seleccionado y el perfil sí tiene "Esenciales", el
  monto se redirige automáticamente a Esenciales.
- Se trackea qué buckets fueron redirigidos y con qué monto.
- La tabla "Plan vs perfil" agrega una nota ámbar al pie cuando hay
  redirecciones, listando qué se redirigió y sugiriendo cambiar de
  perfil (Pago de Deudas / Ahorro Acelerado) si Esenciales se pasa.

Beneficio: el user con perfil Tilbury que tiene deudas ve sus pagos
de deuda contabilizados dentro de Esenciales, con una alerta clara
y transparente, en vez de aparecer como "extra" sin contexto.

### Custom fields del presupuesto: ya existían, ahora son visibles

User: "desde la creacion de un presupuesto yo deberia poder agregar
nuevos elementos en las categorias por ejemplo quisiera agregar
fondo de emergencia, no lo tengo y tambien en entretenimiento
quisiera agregar campos... estos solo deben ser para el usuario
que los crea asi que son custom fields".

Auditado: la funcionalidad **ya existía** desde antes. El botón
"Editar rubros" del step 4 (`planEdit` state) habilita
`addSubcategory` + `addDetail` + `removeDetail`. Los cambios viven
en `planData` y se serializan a `categories_json` del registro
`budgets`. Como el budget está atado a `owner_id`, ya son por user.

Lo que pasaba: no era obvio.

Commit `f0f166e` en `feat/register-split-layout`.

Mejoras de discoverability:

1. **Toolbar del step 4**:
   - El botón cambia a btn-primary teal cuando está activo y muestra
     "✓ Salir de edición" / "✏️ Editar rubros".
   - La hint del lado izquierdo explica explícitamente:
     "¿Falta una categoría (ej. Fondo de emergencia) o un rubro
     (ej. Cafetería)? Activa Editar rubros para personalizarlos.
     Solo se guardan en tu presupuesto."
   - Cuando `planEdit` está activo, la hint cambia a instrucciones:
     "Usa + Nueva categoría al final de cada sección o + Nuevo
     rubro dentro de una categoría."

2. **Copy de los botones existentes**:
   - "+ Agregar subcategoria" → "+ Nueva categoría"
   - "+ Agregar detalle" → "+ Nuevo rubro custom"

### Refactor /gastos en 3 oleadas (task #15)

User: "antes de continuar quiero que revises los registros de
movimientos esto debe funcionar bien ser muy claro, puedes
ayudarme a revisar como esta y ajustarlo... analiza las
posibilidades que tenemos y ajustalo" → "ajusta todo lo que
encontraste seguramente esta bien lo que mencionas".

Audité con agente Explore (~900 palabras de hallazgos): KPI strip
estático, validación de un solo error, catálogo sin "Ingresos",
tabla no ordenable ni paginada, etc.

**Oleada 1** (commit `46650aa`): header dinámico + KPIs filtrados +
filtro de mes + KPIs por moneda.

- `gastos/page.jsx`: "Febrero 2026" hardcoded → "{Mes} {Año}" del
  Date actual. Copy del path: "Registro de Gastos" → "Registro de
  movimientos".
- `expense-register.jsx`:
  - States `monthFilter` (YYYY-MM, default mes actual) y
    `currencyFilter` (default COP, autodetectado).
  - `filteredMovements` filtra por mes y moneda además de los
    filtros previos.
  - `totals` calcula sobre `filteredMovements`. KPIs responden a
    filtros y muestran en la moneda seleccionada.
  - KPI "Movimientos" muestra `filteredMovements.length`.
  - KPI "Disponible" → **"Balance del periodo"** (income−expense)
    con color condicional.
  - Toolbar nueva con `<input type="month">` + botón "Todos los
    meses" + select de moneda.
  - Chip "💰 Ingresos" agregado a los filtros de tipo.

**Oleada 2** (commit `7d8e9fd`): validación multi-error + Ingresos
en catálogo + hint Transfer + MoneyInput.

- `expense-catalog.js`: nueva categoría "Ingresos" con 4 subs
  (Salario, Freelance, Inversiones, Otros ingresos) cada una con
  sus aristas. Resuelve el bug del catálogo inconsistente.
- Validación: muestra TODOS los errores con
  `Swal.fire({ html: <ul>... })`. Título: "Faltan N campos" o
  "Revisa el formulario".
- Hint visual indigo cuando seleccionas Transfer: "Las
  transferencias entre cuentas no usan categoría ni método;
  selecciona origen y destino".
- Input "Valor / Amount" → `<MoneyInput>` (puntos de miles vivos).

**Oleada 3** (commit `539dab0`): tabla ordenable + paginación 25/pág.

- States nuevos: `sortKey` (date|amount|category, default date),
  `sortDir` (asc|desc, default desc), `page` (1, PAGE_SIZE=25).
- `sortedMovements`: useMemo que ordena `filteredMovements` por
  el sort actual. Fecha lex, monto numérico, categoría por
  subcategory.
- `pagedMovements`: slice por página.
- Headers "Fecha", "Categoría", "Valor" son clickables con
  indicador ↑/↓/↕. Página se resetea a 1 cuando cambia un filtro.
- Controles de paginación al pie: "Mostrando A–B de N",
  Anterior/Siguiente, página actual/total. Solo se muestran si
  hay >25 movimientos filtrados.

Decidido NO meter "restante de presupuesto en el form" ni "filtros
en URL" en esta oleada — el módulo ya cumple los objetivos
(claridad, KPIs filtrados, escala). Quedan como mejoras futuras.

### Tareas finales del refactor /gastos + borrar borradores

User: "ha lo que dejaste de hacer quiero todo eso" + "yo deberia
poder borrar los borradores eso no esta siendo posible".

Cuatro commits adicionales:

- `1c394c1` **Filtros en URL**: states inicializados desde
  searchParams, useEffect que sincroniza state → URL con
  `router.replace + scroll:false`. Solo se serializan no-defaults.
  Keys: filter, month, currency, q, from, to, sortKey, sortDir,
  page, sub, method, bank, att.

- `137a251` **Restante de presupuesto en form**: nuevo state
  `budgetForFormDate` cargado vía
  `GET /api/budgets?period={form.date}`. Mapas
  `plannedBySubcategory` y `spentBySubcategory`. Render bajo el
  select de subcategoría con 4 estados visuales (no_in_budget,
  remaining > 15%, remaining < 15%, remaining < 0).

- `b330eb5` (back) + `6fb8e46` (front) **Borrar borradores**:
  - Backend: `deleteUserBudget(userId, budgetId)` que valida
    ownership + status='draft', borra transactions + budget.
    Handler `deleteBudgetById` + ruta DELETE en `handler.js`.
    Solo drafts. 409 si no.
  - Frontend: proxy `/api/budgets/[id]` con DELETE +
    `budget-draft-delete.jsx` (Client). En `/presupuesto`, los
    chips de "Presupuestos guardados" envuelven cada draft con
    botón ✕ rojo.
  - Smoke test end-to-end OK (creé draft → DELETE → summary vacío).

- `1c963f0` **Adjuntos client-side con data URLs**: FileReader
  → readAsDataURL → almacenado como `"nombre|||dataUrl"` en el
  array `attachments[]` (DB ya es TEXT[], no requiere cambios).
  Max 2 MB por archivo. Render como links clicables tanto en el
  form (con botón quitar) como en el listado.

### Cleanup MVP + borrado manual de budget activo

User: "para el MVP todas esas cosas que no estan apuntando a nada
deberian ser eliminadas" + (mientras yo trabajaba en el cleanup)
"acabo de activar presupuesto para este mes y no queria eso
eliminalo el de henry8812@gmail.com".

Commit `9a77008` en `feat/register-split-layout`:
- Sidebar: quitadas entradas "Reglas" y "Tema" (href=#, no
  implementadas).
- /importar: quitadas las cards "Importar bancos" e "Importar
  presupuesto" (ambas placeholders "próximamente"). Queda solo
  la card real de Movimientos.

Borrado manual del budget activo de henry8812@gmail.com periodo
2026-05 vía SQL directo (no se podía desde la UI porque el
endpoint DELETE solo admite drafts, y este estaba 'active'). 
Ejecutado:
- `DELETE FROM transactions WHERE budget_id=14faae74...`
- `DELETE FROM budgets WHERE id=14faae74... AND owner_id=aa780946...`
Confirmado: 0 budgets de 2026-05 restantes para ese user.

### Modelo de estados de presupuesto + UI agrupada + dashboard

User: "por qué querría ver los presupuestos guardados en forma de tags?
si quiero ver solo debe listar primero el activo y si quiero seleccionar
uno futuro o pasado, no puedo editar uno pasado... un presupuesto no
puede estar activo si no es del mes y año en curso... ese presupuesto
vs real debería verlo en el dashboard no en la pantalla de presupuesto...
por qué tengo opciones de atrás y continuar? a dónde deberían llevarme
realmente".

**Backend** (`c9705e4` en `feat/welcome-onboarding`):
- `upsertUserBudget`: si status='active' pero period != currentPeriod,
  fuerza 'draft' (futuro) o 'archived' (pasado).
- Nueva función `reconcileBudgetStatuses(userId)`: UPDATE batch que
  demotea todos los 'active' del user cuyo period != currentPeriod.
  Se llama al inicio de `listUserBudgetSummaries` para que la UI
  refleje el estado correcto siempre.

**SQL inmediato**: junio 2026 de henry8812 que estaba 'active'
incorrectamente → `UPDATE budgets SET status='draft' WHERE id=...`.

**Frontend** (`5563881` y `53b6392` en `feat/register-split-layout`):

1. /presupuesto: reemplazado el bloque de chips por 3 secciones:
   - **Activo · mes en curso**: card destacado con botón Editar.
     Si no hay, panel-soft con "No tienes presupuesto activo para X"
     + botón "Crear presupuesto del mes".
   - **Borradores futuros · editables**: lista con links + botón ✕
     (delete draft, reutilizando endpoint DELETE).
   - **Histórico · solo lectura**: `<details>` colapsable con los
     budgets cuyo period < now, mostrados como texto.

2. Quitada la tabla "Presupuesto vs real" de /presupuesto (ya vive
   en el dashboard). Reemplazada por una línea hint que linkea a `/`.

3. Wizard post-save:
   - active → redirige a `/` (dashboard, donde se ve plan vs real).
   - draft → redirige a `/presupuesto` (lista organizada).
   Antes redirigía a `/presupuesto?period={p}` y dejaba al user
   atrapado en el wizard con botones Atrás/Continuar.

### Patrimonio + intereses EA en cuentas (task #21)

User: "en el dashboard se sumaria lo que tengo o no? por que ahi
debe sumar lo que tengo en cuenta en ahorros y demas es mas
deberia tener en cuenta si mi ahorro me da intereses ea o no,
hay que validar eso por que por ejemplo tengo ahorro en mi cuenta
de global en pesos s me de 11EA y en dolares 8%, esto deberia
intentar calcular o incluso tener una opcion para que yo valide
si es verdad" + "agrega todo y ajusta lo necesario para que
funcione como quiero".

**Backend** (commit `7a596ba` en `feat/welcome-onboarding`):
- Columna nueva `accounts.interest_rate_ea NUMERIC` (nullable) en
  `init-db.sql` + ALTER aditivo.
- Aplicada la migración a DB local.
- `mapRow` expone `interestRateEa`; `normalizeAccountPayload`
  valida; `createAccount`/`updateAccount` persisten.
- Routes pasan `body.interestRateEa`.

**Lección operacional**: el ESM `import("./handler.js")` del
local-server.js cachea módulos. Cuando edito archivos del back,
debo matar el proceso COMPLETAMENTE (kill -9) y relanzar para
que tome los cambios. `nohup npm run dev` mientras hay otro
proceso vivo dispara EADDRINUSE silencioso.

**Frontend** (commit `b3063ee` en `feat/register-split-layout`):
- /bancos: input "Interés EA %" opcional en form de cuenta.
- /configurar-realidad AccountsStep: columna nueva en el grid.
- dashboard-home: card nueva "💰 Patrimonio" al inicio del tab
  "flujo":
  - Cards por moneda (COP, USD, EUR…) con total + interés/año
    estimado.
  - Tabla detalle de cuentas con tasa: saldo, %, interés/año.
  - Nota ámbar: "Proyección teórica, confirma con tu banco antes
    de tomar decisiones".
  - Si no hay cuentas con tasa, hint hacia /bancos.

Cálculo: `interés_anual = balance × (tasa_EA / 100)` por cuenta,
suma por moneda. Si la cuenta no tiene tasa, no se incluye en la
proyección (pero sí en el total).

### Acreditación automática de intereses (B) + rediseño dashboard

User: "me gusta la B, vamos con esa opcion" + "rehagas el dashboard
hay mucha información del mes en curso y los datos que son cambiantes
mes a mes... 2 visuales vista sencilla que es por defecto y la otra
avanzada... esfuerzate esmerate mucho en eso busca referentes y demas".

Investigué referentes 2026 (Eleken, WildnetEdge, BSuperb, Econumo)
— el patrón general: 4 superficies (balance/net-worth, cash flow,
transactions, budgets/goals); jerarquía visual fuerte; card-based
layout; mobile-first; AI insights opcionales; responsive.

**Backend** (commit `a942ae4` en `feat/welcome-onboarding`):
- Nuevo módulo `src/lib/server/interest-accrual.js`.
- `accrueInterestForUser({userId, period})` recorre cuentas con
  `interest_rate_ea > 0`, calcula
  `interes = balance × ((1 + EA/100)^(1/12) - 1)`, crea movement
  income con destination_account_id = cuenta (auto-suma al
  balance), idempotente vía tag `auto-interest:{accountId}:{period}`.
- Endpoint `POST /accounts/accrue-interest` (body opcional
  `{period: "YYYY-MM"}`, default = mes anterior).
- handler.js: ruta nueva colocada ANTES del matcher /accounts/:id.
- Smoke test: 5M @ 11% EA → +43.673/mes. Reintento → skipped.

**Frontend** (commits `5c9895f` proxy + `0d9294a` auto-accrual +
`71c9931` rediseño en `feat/register-split-layout`):
- Proxy `/api/accounts/accrue-interest`.
- Dashboard: `useEffect` post-load llama el endpoint si hay cuentas
  con tasa. Si hubo accrued, refetch accounts + expenses. Banner
  verde teal "✓ Intereses {período} acreditados".
- Nuevo state `viewMode` ("simple"|"advanced", default "simple",
  persistido en localStorage `budgetapp_dashboard_view`).
- Toggle visible arriba de las tabs: 🪟 Sencilla / 📊 Avanzada.

**Vista Sencilla (default)**:
- KPI strip + Patrimonio.
- 🔔 Alertas rápidas: runway <30 días, sobre plan del mes, metas
  en riesgo (deadline ≤30d con progreso <80%).
- 📊 Top 3 categorías de gasto del mes con barras %.
- Últimos 5 movimientos con prefijo +/− y color.
- Footer: link a vista avanzada.

**Vista Avanzada**: mantiene las tabs Flujo / Presupuesto / Metas
existentes con gráficos, presupuesto vs real, realidad, metas.

Cálculos nuevos: `avgDailyExpense` (últimos 3 meses) y
`topCategoriesMonth`.

### Seed realista + rediseño profesional del dashboard

User: "el dashboard no se ve nada profesional, hay muchas referencias
financieras hermosas, esto no se parece a nada... carga de datos el
usuario henry8812@gmail.com con el mes de mayo 2026".

**Seed** (`/tmp/seed-henry8812.sql`): para `henry8812@gmail.com`:
- 4 cuentas: Global66 COP (8.5M @ 11% EA), Bancolombia Corriente
  (1.2M), Global66 USD (USD 1200 @ 8%), Efectivo (150K).
- 2 deudas: Tarjeta Visa Bancolombia (2.8M @ 32.5%) y Crédito
  vehículo (8.5M @ 18%).
- 3 metas activas: Fondo emergencia ($4.2M/$15M), Vacaciones 2026
  ($1.8M/$4.5M), Cambio de carro ($5.5M/$35M).
- 29 movimientos de mayo 2026 (arriendo, servicios, mercados, gas,
  taxis, restaurantes, plataformas, deudas, salud, ahorros, 2
  ingresos).
- Presupuesto **activo** de mayo 2026 con categorías y totales
  consistentes (perfil mark-tilbury).

**Rediseño profesional** (commit `60a8feb` en
`feat/register-split-layout`): basado en patrones de Copilot Money
+ Monarch Money.

- 3 KPIs viejos ("Disponible/Gastos/Real vs plan") solo se muestran
  en vista Advanced. Simple los reemplaza por:
- **Hero KPI grid (4 cards)**: Patrimonio, Ingresos, Gastos, Ahorro
  del mes. Cada uno con font 22px Syne, sub-line con contexto
  (intereses auto-acreditados, plan vs real, % ahorro).
- **Flujo del mes**: 3 barras horizontales (Ingresos teal, Gastos
  rojo, Ahorro violet) con monto a la derecha + footer "Resultado
  del mes" en color condicional.
- **Alertas**: card con borde-izquierda coloreado (rojo crítico,
  ámbar warning) sin saturación de emojis.
- **Dónde gastas más**: top 3 categorías numeradas con paleta
  variada (teal/violet/cyan).
- **Movimientos recientes**: lista 6 items con divider sutil, signo
  +/− y color por tipo.
- **Tus metas**: cards laterales con progreso, ribbon rojo at-risk.
- Footer pequeño con link a vista avanzada.

Estilos inline: cards blancas border #e5e7eb radius 14 padding 18,
Syne en titulares, generosa whitespace, paleta limitada (teal,
rojo, violet, ámbar, grises).

### Seed abril+marzo + Advanced dashboard con peso

User: "la vista sencilla me gusta... la avanzada en cambio le falta
mucho... crea presupuesto de abril 2026 agrégale datos y muestra más,
es importante que la vista avanzada tenga peso".

**Seed adicional**: `/tmp/seed-abril-henry8812.sql`. Para
`henry8812@gmail.com`:
- **Presupuesto abril 2026** archivado con totals coherentes.
- **25 movimientos de abril** (arriendo, servicios, mercados,
  transporte, restaurantes, deudas, salud, 3 ahorros).
- **13 movimientos de marzo** (sin presupuesto, para tendencia
  histórica).

Estado: abril archived, mayo active, junio draft. 3 meses de data.

**Vista Avanzada rediseñada** (commit `3eb1493` en
`feat/register-split-layout`):

1. **KPI cards con sparkline + trend %**: 4 cards (Patrimonio,
   Ingresos, Gastos, Ahorro). Trend vs promedio 3M con badge
   teal/rojo. Sparkline SVG polyline.

2. **Flujo 6 meses (chart)**: barras de 3 colores por periodo
   (income teal / expense rojo / saving violet). Mes activo
   resaltado. Etiqueta es-CO.

3. **Patrimonio detallado**: tabla por cuenta con saldo, % del
   total por moneda, tasa, interés/año. Chips de moneda indigo.

4. **Análisis por categoría (top 6)**: total mes vs avg 3M, delta
   % color-coded, sparkline 3M + actual.

5. **Próximos compromisos**: deudas con due_date ≤60d, urgentes
   en rojo.

6. **Heatmap de gasto diario**: grid 7-col, intensidad teal por día.

7. **Metas activas**: cards con progreso, aporte sugerido, fecha
   objetivo.

8. **Últimos 15 movimientos**: tabla completa.

9. **Layout clásico** (tabs viejas) en `<details>` colapsado al
   final como legacy.

Soporte: carga adicional `/api/debts`, useMemos `trends3M`,
`categoryAnalysis`, `dailySpend`. Helper inline `Sparkline` (SVG).

### Heatmap mejorado (paleta verde→rojo + modal click)

User: "creo que es un mapa de calor... más apropiado como mapa de
calor que vaya pasando de verde a rojo y cuando yo dé clic a algún
día se abra un modal con el detalle".

Commit `c2e8f2e` en `feat/register-split-layout`.

- Paleta de 6 stops: gris (sin gasto) → verde claro → verde →
  amarillo → naranja → rojo, según intensidad (gasto/max del mes).
- Leyenda visual al lado del título con 5 cuadritos "menos / más".
- Color del texto del número adaptado al fondo (verde oscuro o
  blanco según contraste).
- Cada celda es ahora `<button>` con hover `scale(1.08)`. Días sin
  gasto quedan disabled (cursor default).
- onClick → modal Swal con 3 secciones color-coded:
  - Ingresos (teal, prefijo +)
  - Gastos (rojo, prefijo −)
  - Ahorro/Inversión (violet, prefijo +)
  Cada item: detalle, subcategoría · método, monto. Header del
  modal: "DD MesAbrev YY".
- Si el día clickeado no tiene movimientos: modal info "No hay
  movimientos registrados este día".

Import nuevo: `Swal` de sweetalert2.

### Ingresos vinculados a cuenta destino (bola de nieve real)

User: "si mis ingresos llegan a mi cuenta de global66 en pesos eso
quiere decir que mis ingresos con una fecha en particular se suman
y aumenta en tipo bola de nieve por el interés... pero como tal yo
recibo ingresos pero no digo donde los recibo o en que moneda... es
algo que hay que ajustar".

Commit `684f5a8` en `feat/register-split-layout`.

El campo "Destino ingreso (opcional)" ya existía en el form de
income pero era opaco. Mejoras:

- Label "Cuenta destino del ingreso *" (asterisco rojo, sigue
  técnicamente opcional para efectivo).
- Default "Efectivo (no se vincula a cuenta)" explícito.
- Al elegir cuenta, la **moneda del movement se hereda
  automáticamente**.
- Hint dinámico debajo del select:
  - Con cuenta (teal): "✓ se sumará al saldo · bola de nieve 🌱
    sobre los próximos intereses".
  - Sin cuenta (ámbar): "⚠️ no aumenta saldo ni genera intereses".

**Backfill SQL inmediato**: los 7 ingresos COP previos de
henry8812 (marzo+abril+mayo) vinculados a Global66 + recalculo
de balance:

  balance = 8.5M inicial + sum(ingresos vinculados COP) = 26.15M

Próxima acreditación mensual se calculará sobre ese balance crecido
→ efecto bola de nieve real ya funcionando.

### Tarjeta débito completa: form condicional + auto-link source

User: "deberías agregar el tema de tarjeta débito, solo tienes
crédito en las opciones de gastos y yo solo tengo tarjeta de
débito".

Diagnóstico: el schema soportaba `card_type='debit'` y el select
en /bancos tenía la opción, pero el flujo era confuso: el form
pedía cupo/disponible aún para débito, y al usar una débito en un
gasto no se descontaba del saldo de ninguna cuenta.

**Backend** (`218be92` en `feat/welcome-onboarding`):
- Nueva columna `cards.linked_account_id` (UUID nullable
  REFERENCES accounts ON DELETE SET NULL) + ALTER aditivo.
- `normalizeCardPayload`: si `cardType="debit"` fuerza
  `creditLimit/availableCredit = null` y solo conserva
  `linkedAccountId` si es débito.
- `SELECT_COLS` extraído como constante, `mapRow` expone
  `linkedAccountId`.
- Routes POST/PUT pasan `body.linkedAccountId`.

**Frontend** (`605c0f9` en `feat/register-split-layout`):
- `/bancos` form de tarjeta:
  - Débito → select "Cuenta vinculada" + oculta Cupo/Disponible.
    Hint teal: "Los gastos descontarán del saldo de la cuenta".
  - Crédito/Prepago → Cupo y Disponible.
- `/gastos` selector de tarjeta:
  - Cada option muestra `[💳 Débito]` o `[💎 Crédito]` antes del
    nombre.
  - Al elegir débito con linkedAccountId, se hace patch automático
    a `form.sourceAccountId` (así createExpense ya descuenta el
    balance).
  - Hint debajo: débito vinculada "✓ Descontará de {cuenta}",
    débito sin vincular "⚠️ Vincúlala en Bancos", crédito
    "💎 No descuenta — acumula en deuda".

### Pendiente al cierre de este turno

- User va a revisar `/register` en mobile/desktop.
- Si aprueba, próximo paso podría ser: aplicar el mismo patrón split
  a `/login` y `/forgot-password` para consistencia, o mergear
  ambas ramas a main vía PR.
- Sigue pendiente: confirmar los % reales de Tilbury (75/15/10 vs
  50/25/15/10) y consolidar los perfiles en un solo archivo
  compartido.
